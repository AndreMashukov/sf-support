---
name: grill-with-docs
description: A relentless interview to sharpen a plan or design, which also creates docs (ADRs and glossary) as we go.
disable-model-invocation: true
---

Run a `/grilling` session, using the `/domain-modeling` skill.

Read `CONTEXT.md` at the repo root before starting. Update it inline as terms are resolved. Offer ADRs in `docs/adr/` only when the domain-modeling criteria are met.
